# 项目设计文档

> 本文档为项目唯一设计文档，新功能以章节追加。

---

## 1. 喝水提醒托盘应用

### 1.1 架构设计

#### 1.1.1 模块划分

- `App` 启动层：负责单实例启动、依赖注入、托盘组件初始化、窗口生命周期管理。
- `Views`
  - `HydrationSettingsWindow`：唯一主操作窗口，用于查看和修改提醒设置。
  - `HydrationReminderWindow`：右下角提醒弹窗，支持手动关闭和自动关闭。
- `ViewModels`
  - `HydrationSettingsViewModel`：维护设置页状态、保存设置、立即测试提醒。
  - `HydrationReminderViewModel`：维护提醒文案、倒计时显示、关闭动作。
- `Services`
  - `ITrayService`：托盘图标、菜单、双击打开设置、退出应用。
  - `IReminderSchedulerService`：基于 `DispatcherTimer` 进行提醒调度。
  - `IReminderPopupService`：统一创建和关闭提醒弹窗。
  - `ISettingsService`：读写提醒配置，负责默认值回填。
- `Repositories`
  - `ISettingsRepository`：基于 JSON 文件持久化提醒设置。
- `Models`
  - `HydrationSettings`：提醒间隔、弹窗自动关闭秒数、启用状态、最近提醒时间等领域模型。
  - `HydrationSettingsDto`：阶段 1 输出的稳定契约。

#### 1.1.2 数据流向

1. 应用启动后创建托盘图标，不主动显示主窗口。
2. `ISettingsService` 读取本地 JSON 配置；若文件不存在，则回填默认配置：
   - 喝水提醒开启
   - 提醒间隔 30 分钟
   - 弹窗自动关闭 5 秒
3. `IReminderSchedulerService` 使用 `DispatcherTimer` 每秒检查一次是否到达下次提醒时间。
4. 到达提醒时间后，由 `IReminderPopupService` 打开右下角提醒弹窗。
5. 用户可通过弹窗关闭按钮关闭，也可等待 5 秒自动关闭。
6. 用户在设置窗口修改配置并保存后，`ISettingsService` 写入 JSON，调度服务即时重载。
7. 用户通过托盘菜单选择“退出应用”时，先释放计时器和托盘资源，再退出进程。

#### 1.1.3 技术决策

- 仅面向 Windows，采用 `.NET 10 + WPF + Wpf.Ui + CommunityToolkit.Mvvm`。
- 托盘使用 `NotifyIcon` 实现，菜单至少包含“打开设置”“立即提醒一次”“退出应用”。
- 定时调度使用 `DispatcherTimer`，避免引入额外后台线程复杂度。
- 提醒弹窗使用独立 WPF Window，自带显示动画和自动关闭计时器。
- 配置持久化采用 JSON 文件，存放于 `%LocalAppData%/MiniNotifier/hydration-settings.json`。
- 发布采用“框架依赖”模式，并开启裁剪，目标是减小安装包体积。
- 为避免重复提醒和资源浪费，应用需支持单实例运行。

---

### 1.2 功能需求

#### 1.2.1 页面与入口

| 页面 / 入口 | 说明 |
| ----------- | ---- |
| 系统托盘图标 | 应用主入口，启动后仅显示托盘图标，不在任务栏保留主窗口 |
| 设置窗口 | 查看与修改喝水提醒配置，展示当前状态和下次提醒时间 |
| 提醒弹窗 | 右下角浮出提醒窗口，展示喝水提示与关闭操作 |

#### 1.2.2 功能点明细

1. 后台与托盘
   - 应用启动后默认最小化到托盘，不主动弹出主窗口。
   - 托盘图标悬停时显示应用名称与当前提醒状态。
   - 托盘菜单支持打开设置、立即提醒一次、退出应用。
   - 双击托盘图标打开设置窗口。

2. 喝水提醒配置
   - 默认提醒间隔为 30 分钟。
   - 用户可修改提醒间隔，单位为分钟。
   - 用户可开启或关闭喝水提醒。
   - 用户可修改弹窗自动关闭时长，默认 5 秒。
   - 保存后立即生效，并持久化到 JSON 文件。

3. 提醒弹窗
   - 到达提醒时间时在屏幕右下角显示。
   - 弹窗显示提醒标题、简短说明、当前时间、关闭按钮。
   - 用户未操作时，倒计时结束自动关闭。
   - 用户点击关闭后立即消失，不影响下一轮提醒。

4. 状态展示
   - 设置窗口展示：
     - 当前提醒是否启用
     - 当前提醒间隔
     - 当前自动关闭秒数
     - 上次提醒时间
     - 下次预计提醒时间
   - 当配置文件不存在时，以默认值初始化并提示“已加载默认配置”。

#### 1.2.3 业务规则

- 提醒间隔允许范围：`5 - 240` 分钟。
- 自动关闭秒数允许范围：`3 - 15` 秒。
- 当提醒被关闭时，不再弹窗，但仍保留托盘图标和设置入口。
- 保存配置成功后，下一次提醒时间按“保存时刻 + 新间隔”重新计算。
- 立即提醒一次不会改写下一次正常提醒时间。
- 退出应用前必须释放 `NotifyIcon` 和所有定时器。
- 配置写入失败时，不更新内存中的已保存状态，并提示用户重试。

#### 1.2.4 关键状态

- `loading`：启动时读取配置，设置窗口进入加载态。
- `empty`：首次启动无配置文件，显示默认配置说明态。
- `error`：配置读取或保存失败，显示错误说明和重试按钮。
- `no-permission`：配置目录无法写入时，提示无权限并禁用保存按钮。

---

### 1.3 界面原型

#### 1.3.1 托盘菜单

```text
┌ MiniNotifier 托盘菜单 ───────────────────────┐
│ 打开设置                                    │
│ 立即提醒一次                                │
│ 分隔线                                      │
│ 退出应用                                    │
└─────────────────────────────────────────────┘
```

#### 1.3.2 设置窗口 - 默认态

```text
┌──────────────── MiniNotifier 喝水提醒 ────────────────┐
│ [应用图标] MiniNotifier                               │
│ 轻量后台提醒，帮助你保持喝水节奏                       │
│                                                       │
│ [状态卡片]                                            │
│ 已启用 | 上次提醒：19:00 | 下次提醒：19:30            │
│                                                       │
│ [表单卡片]                                            │
│ 喝水提醒      [开启  v]                               │
│ 提醒间隔      [ 30 ] 分钟                             │
│ 自动关闭      [  5 ] 秒                               │
│                                                       │
│ [测试提醒]                           [保存设置]       │
│                                                       │
│ 提示：应用关闭窗口后仍会驻留系统托盘。                 │
└───────────────────────────────────────────────────────┘
```

#### 1.3.3 设置窗口 - loading

```text
┌──────────────── MiniNotifier 喝水提醒 ────────────────┐
│ [应用图标] MiniNotifier                               │
│                                                       │
│ [ ProgressRing ] 正在加载提醒配置...                  │
└───────────────────────────────────────────────────────┘
```

#### 1.3.4 设置窗口 - empty

```text
┌──────────────── MiniNotifier 喝水提醒 ────────────────┐
│ [应用图标] MiniNotifier                               │
│                                                       │
│ 尚未检测到本地配置                                    │
│ 已为你载入默认喝水提醒：每 30 分钟提醒一次            │
│                                                       │
│ [使用默认配置继续]                   [打开设置表单]    │
└───────────────────────────────────────────────────────┘
```

#### 1.3.5 设置窗口 - error / no-permission

```text
┌──────────────── MiniNotifier 喝水提醒 ────────────────┐
│ [应用图标] MiniNotifier                               │
│                                                       │
│ 配置加载失败 / 无法写入配置文件                        │
│ 请检查本地目录权限后重试。                             │
│ 错误码：CFG-001 / CFG-002                             │
│                                                       │
│ [重试]                                 [关闭窗口]     │
└───────────────────────────────────────────────────────┘
```

#### 1.3.6 右下角提醒弹窗

```text
┌────────────── 喝水提醒 ──────────────┐
│ 该喝水了                             │
│ 起来活动一下，喝几口水。             │
│ 当前时间：19:30                      │
│                                      │
│ 5 秒后自动关闭        [关闭]         │
└──────────────────────────────────────┘
```

---

### 1.4 交互说明

#### 1.4.1 启动流程

1. 用户启动应用。
2. 应用检测单实例；若已有实例运行，则激活已有设置窗口或仅提示已在托盘运行。
3. 应用加载配置并创建托盘图标。
4. 若启动参数不要求打开界面，则主窗口不显示，直接进入后台运行。
5. 调度服务开始计时，等待下一次提醒。

#### 1.4.2 托盘交互流程

1. 用户右键托盘图标，打开菜单。
2. 选择“打开设置”后显示设置窗口。
3. 选择“立即提醒一次”后立即弹出提醒窗口。
4. 选择“退出应用”后执行资源释放并结束进程。

#### 1.4.3 设置保存流程

1. 用户在设置窗口修改提醒开关、间隔或自动关闭时长。
2. 前端先执行表单校验：
   - 间隔必须为 5 至 240 的整数
   - 自动关闭必须为 3 至 15 的整数
3. 校验通过后调用保存命令。
4. 保存成功：
   - 写入 JSON
   - 更新界面状态
   - 调度器重算下一次提醒时间
   - 弹出成功提示
5. 保存失败：
   - 保留用户输入
   - 显示错误提示
   - 不覆盖当前已生效配置

#### 1.4.4 提醒弹窗流程

1. 调度器检测到到期时间，触发提醒。
2. 弹窗从右下角进入，默认置顶但不抢占焦点。
3. 用户可以点击关闭按钮立即关闭。
4. 若无操作，则在倒计时结束后自动关闭。
5. 弹窗关闭后，调度器按提醒间隔计算下一次触发时间。

#### 1.4.5 边界场景

- 首次运行无配置文件：自动生成默认配置并进入 empty 提示态。
- 配置文件损坏：回退到 error 态，允许用户重试或恢复默认值。
- 系统时间被修改：下一轮检查按“当前时间 >= 下次提醒时间”重新判断。
- 设置窗口被关闭：应用继续后台运行，不退出。
- 用户禁用提醒：清除待触发提醒，仅保留托盘入口。
- 无权限写入配置目录：进入 no-permission 态，允许只读查看当前内存配置。

#### 1.4.6 错误处理方式

- 配置读取失败：提示“无法读取提醒配置”，提供重试。
- 配置保存失败：提示“保存失败，当前设置未生效”，保留编辑内容。
- 托盘初始化失败：记录日志并中止启动，避免进入不可退出状态。
- 弹窗创建失败：记录日志，保留下一轮提醒，不让调度器崩溃。

---

### 1.5 阶段门控结论

- 当前处于：`阶段 0 - 设计文档`
- 本章节已覆盖架构设计、功能需求、界面原型、交互说明
- 待用户确认本设计文档后，方可进入“开始视图开发”
