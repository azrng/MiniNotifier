# 修复提醒窗外层背景导致的方角边框

## 本次目标
- 让提醒窗真正显示为透明外壳上的圆角卡片，去掉外层 html/root 背景导致的方角白边。

## 核心改动
- 更新 `src-ui/app/App.tsx`，将窗口类型同步写入 `document.documentElement` 与 `document.body`。
- 更新 `src-ui/styles/index.css`，在 `main` / `reminder` 窗口下把 `:root`、`html`、`body`、`#root` 这一层统一设为透明背景。
- 更新 `TASK.md`，记录并收口本次任务状态。
- 重新执行一次 `npx tauri build --debug`，生成新的调试安装包供验证。

## 修改文件
- `src-ui/app/App.tsx`
- `src-ui/styles/index.css`
- `TASK.md`

## 校验情况
- 已执行 `npm run build`
- 已执行 `npx tauri build --debug`
- 已确认最新 debug 安装包产物时间：
  - `src-tauri/target/debug/bundle/msi/MiniNotifier_0.1.0_x64_en-US.msi` -> `2026-04-18 15:22:53`
  - `src-tauri/target/debug/bundle/nsis/MiniNotifier_0.1.0_x64-setup.exe` -> `2026-04-18 15:23:06`

## 风险或遗留项
- 本次仍是 debug 安装包验证链路；如果这版提醒窗圆角外观符合预期，建议再补一次 release 打包。
- 若安装后仍能看到极细边框，下一步需要进一步检查 WebView2 宿主层是否还在绘制默认背景。
