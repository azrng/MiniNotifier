# 启动显示与单实例修正

## 本次目标

- 调整 MiniNotifier 启动行为：用户双击启动时直接打开设置页面。
- 防止应用重复启动，避免多开窗口、托盘图标和提醒调度器。

## 核心改动

- 在 `App` 启动入口增加本地命名 `Mutex` 单实例保护。
- 隐藏更新模式仍优先执行，不受单实例保护影响。
- 首次普通启动完成 Host、托盘和调度器初始化后，主动显示设置窗口。

## 修改文件

- `src/MiniNotifier/App.xaml.cs`
- `TASK.md`
- `doc/devlog/2026-04-28-启动显示与单实例修正.md`

## 校验情况

- `dotnet build MiniNotifier.slnx` 通过，0 警告。
- 通过预先持有 `Local\MiniNotifier.SingleInstance` 互斥锁模拟已有实例，二次启动在 5 秒内退出，退出码 0。
- `dotnet run --project src\MiniNotifier\MiniNotifier.csproj -- --apply-update` 缺少请求文件时按预期退出码 1。
- `dotnet publish src\MiniNotifier\MiniNotifier.csproj -c Release` 通过。

## 风险或遗留项

- 当前策略是第二次启动直接退出，不主动唤起已运行实例窗口；符合本次“不需多开”的要求。
