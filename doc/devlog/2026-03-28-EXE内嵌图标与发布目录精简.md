# 2026-03-28 EXE内嵌图标与发布目录精简

## 本次目标
- 将应用图标改为从程序集资源读取，避免发布目录额外携带 `Assets\Icons`

## 核心改动
- 将 `AppIcon.ico` 从输出内容改为 WPF 资源
- 调整 `AppIconProvider`，托盘图标和窗口图标都从程序集资源加载
- 保留 `ApplicationIcon` 配置，让 EXE 继续内嵌应用图标

## 修改文件
- `src/MiniNotifier/MiniNotifier.csproj`
- `src/MiniNotifier/Helpers/AppIconProvider.cs`
- `TASK.md`

## 校验情况
- 执行 `dotnet build MiniNotifier.slnx`，构建成功
- 执行 `dotnet publish .\src\MiniNotifier\MiniNotifier.csproj -c Release -r win-x64 --self-contained false -o .\artifacts\publish-icon-check`，发布成功
- 检查发布目录，已不再生成 `Assets` 文件夹

## 风险或遗留项
- 当前为框架依赖发布，发布目录仍会包含多个运行时依赖 DLL；如果需要尽量收敛为单文件，还需额外开启单文件发布配置
