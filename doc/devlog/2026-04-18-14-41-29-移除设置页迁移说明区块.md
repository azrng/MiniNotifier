# 移除设置页迁移说明区块

## 本次目标
- 删除设置页右侧的迁移状态与关闭提示说明区域，让界面只保留核心设置内容。

## 核心改动
- 更新 `src-ui/features/settings/SettingsWindow.tsx`，移除右侧 `aside` 中的“迁移状态”卡片和“托盘常驻”提示卡片。
- 同步把对应区域的双栏布局改回单列，避免删除说明区后页面右侧留下空白列。
- 更新 `TASK.md`，记录并收口本次任务状态。

## 修改文件
- `src-ui/features/settings/SettingsWindow.tsx`
- `TASK.md`

## 校验情况
- 已执行 `npm run build`
- 已重新读取改动片段，确认删除区域和外层布局一致

## 风险或遗留项
- 本次未额外执行 Tauri 安装包构建，只完成前端构建校验；如需确认桌面壳中的最终视觉效果，仍建议再跑一次应用级 smoke test。
