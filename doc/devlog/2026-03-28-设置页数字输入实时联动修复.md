# 2026-03-28 设置页数字输入实时联动修复

## 本次目标
- 修复设置页中数字框已改值，但顶部提醒频率角标仍显示旧值的问题

## 核心改动
- 为提醒间隔和自动关闭秒数的 `NumberBox` 双向绑定补充 `UpdateSourceTrigger=PropertyChanged`
- 让输入框数值在编辑时实时回写到 ViewModel，而不是等失焦后才同步

## 修改文件
- `src/MiniNotifier/Views/Windows/MainWindow.xaml`
- `TASK.md`

## 校验情况
- 执行 `dotnet build MiniNotifier.slnx -c Release`，构建成功
- 执行 `dotnet build MiniNotifier.slnx` 时因本机正在运行 `MiniNotifier.exe` 导致 Debug 输出被锁定，非代码错误

## 风险或遗留项
- 当前已完成编译验证；仍建议在本机实际修改一次提醒间隔，确认顶部频率角标和保存结果都符合预期
