# 2026-03-28 下次提醒时间实时预估修复

## 本次目标
- 修复设置页中修改提醒间隔后，“下次提醒”仍显示旧保存时间的问题

## 核心改动
- 为 `MainWindowViewModel` 增加基于当前编辑值的下次提醒实时预估逻辑
- 当提醒开关、暂停状态、提醒间隔变化时，立即刷新 `NextReminderText`
- 保留已保存配置加载逻辑，避免初始化时被实时预估覆盖

## 修改文件
- `src/MiniNotifier/ViewModels/Windows/MainWindowViewModel.cs`
- `TASK.md`

## 校验情况
- 执行 `dotnet build MiniNotifier.slnx -c Release`，构建成功

## 风险或遗留项
- 当前“下次提醒”在编辑阶段显示的是按当前输入值计算的预估时间；真正持久化后的正式时间仍以保存结果为准
