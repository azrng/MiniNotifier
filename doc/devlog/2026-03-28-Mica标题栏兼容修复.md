# 2026-03-28 Mica 标题栏兼容修复

## 本次目标

- 修复托盘打开设置时由于 `FluentWindow` Mica 配置不兼容导致的异常退出

## 核心改动

- 为主窗口补充 `ExtendsContentIntoTitleBar="True"`
- 保留 Mica 背景效果，同时满足 `Wpf.Ui` 对 `FluentWindow` 的要求
- 通过日志确认异常根因为窗口 Backdrop 配置冲突

## 修改文件

- `src/MiniNotifier/Views/Windows/MainWindow.xaml`

## 校验情况

- 已读取本地异常日志定位根因
- 已执行 `dotnet build MiniNotifier.slnx`
- 已执行短时启动验证

## 风险或遗留项

- 若后续继续自定义标题栏区域，需要再检查拖拽与命中测试行为
