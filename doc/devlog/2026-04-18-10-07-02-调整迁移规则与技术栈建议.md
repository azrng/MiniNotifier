# 调整迁移规则与技术栈建议

## 本次目标
- 修订 AI 规则，使其更符合当前仓库“现状仍是 WPF、目标逐步迁移到 Tauri”的真实状态。
- 收敛默认技术栈，避免把小型托盘工具按全新 Web 全栈桌面项目一次性铺满。

## 核心改动
- 在 `AGENTS.md` 中补充“当前实现与迁移目标”双态说明，明确现有 WPF 代码仍是当前事实来源。
- 调整根规则中的迁移原则，强调原生能力优先验证、简单持久化优先、避免默认全家桶铺栈。
- 在 `frontend-AGENTS.md` 中将前端技术栈改为“React + TypeScript + Vite + 按需引入路由/查询/全局状态”，并收敛组件基础设施默认值。
- 在 `backend-AGENTS.md` 中将后端技术栈改为“默认轻持久化，按需升级 SQLite”，同时把系统集成能力与持久化分层拆开。

## 修改文件
- `AGENTS.md`
- `frontend-AGENTS.md`
- `backend-AGENTS.md`
- `TASK.md`

## 校验情况
- 已按 UTF-8 重新读取 `AGENTS.md`、`frontend-AGENTS.md`、`backend-AGENTS.md`，确认关键中文和规则内容正常。
- 本次仅修改规则与文档，未执行 `build`、`lint`、`typecheck` 或运行时验证。

## 风险或遗留项
- 当前规则已切到“渐进迁移”语境，但实际迁移设计文档和实施顺序仍需后续继续细化。
- `frontend-AGENTS.md` 与 `backend-AGENTS.md` 目前是目标态规则，真正开始迁移前仍建议补一次原生能力 spike 设计说明。
