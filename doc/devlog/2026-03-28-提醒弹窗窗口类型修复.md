# 2026-03-28 提醒弹窗窗口类型修复

## 本次目标

- 修复“立即提醒一次”时弹窗创建失败的问题

## 核心改动

- 通过日志定位到 `HydrationReminderWindow` 使用 `FluentWindow + AllowsTransparency=True` 的组合冲突
- 将提醒弹窗从 `FluentWindow` 改为普通 `Window`
- 保留现有圆角、透明背景、动画和右下角浮出效果

## 修改文件

- `src/MiniNotifier/Views/Windows/HydrationReminderWindow.xaml`

## 校验情况

- 已读取本地日志确认根因
- 已执行 `dotnet build MiniNotifier.slnx`
- 已执行短时启动验证

## 风险或遗留项

- 当前提醒弹窗为了稳定性使用原生 `Window`
- 如果后续还想进一步增强视觉，可在不引入 `FluentWindow` 冲突的前提下继续美化
