# 2026-03-28 设置界面简化

## 本次目标

- 将设置界面改为更简单、更克制的风格
- 让右上角只保留关闭按钮，去掉最小化和最大化

## 核心改动

- 将设置窗口从偏展示型布局改为简洁的三段式布局：
  - 顶部标题栏
  - 中部主设置卡片
  - 底部状态摘要卡片
- 去掉大面积背景氛围元素，整体更轻量
- 使用 `TitleBar` 控件显式控制右上角按钮，仅保留关闭按钮
- 窗口改为 `ResizeMode="NoResize"`，避免出现多余的系统窗口操作

## 修改文件

- `src/MiniNotifier/Views/Windows/MainWindow.xaml`

## 校验情况

- 已执行 `dotnet build MiniNotifier.slnx`
- 已执行短时启动验证

## 风险或遗留项

- 当前是简化版视觉，后续若需要可继续微调间距、字号和卡片层级
