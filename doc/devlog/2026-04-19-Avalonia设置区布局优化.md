# 2026-04-19 Avalonia 设置区布局优化

## 本次目标

- 优化 Avalonia 主窗口“基础设置 / 操作入口”区域的布局密度和对齐关系。

## 核心改动

- 将基础设置从散列表单改为卡片式设置行，每行左侧为标题与说明，右侧为固定宽度控制区。
- 输入框与开关统一右侧对齐，降低控件漂浮感。
- 新增 `setting-row` 和 `action-fill` 样式，统一设置行和操作按钮布局。
- 操作入口按钮改为全宽按钮，增强右侧操作区的稳定性和可点击性。

## 修改文件

- `src/MiniNotifier.Avalonia/Views/MainWindow.axaml`
- `src/MiniNotifier.Avalonia/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况

- 已执行 `dotnet build src/MiniNotifier.Avalonia/MiniNotifier.Avalonia.csproj -c Release`，构建成功，0 警告，0 错误。
- 已执行 `dotnet build MiniNotifier.slnx -c Release`，解决方案构建成功，0 警告，0 错误。

## 风险或遗留项

- 未做自动化截图回归；建议重新打开主窗口确认基础设置区控件对齐、右侧按钮宽度和窄高窗口滚动表现。
