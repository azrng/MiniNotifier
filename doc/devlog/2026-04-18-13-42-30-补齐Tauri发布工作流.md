# 补齐 Tauri 发布工作流

## 本次目标
- 在保留旧 WPF 回滚产物的前提下，把当前 Tauri 迁移主线接入 CI 构建与安装包产物输出。
- 补齐仓库入口说明，明确当前默认开发路径和发布结果。

## 核心改动
- 更新 `.github/workflows/build.yml`，在原有 WPF 打包基础上新增 Node 24、Rust、前端依赖安装和 `npx tauri build`，并上传 Tauri 的 MSI 与 NSIS 产物。
- 新增 `README.md`，说明当前仓库处于 WPF 到 Tauri 的渐进迁移阶段，并补充本地开发、回滚链路和 CI 产物说明。
- 同步更新 `TASK.md` 记录本次交付链路补齐任务。

## 修改文件
- `.github/workflows/build.yml`
- `README.md`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build MiniNotifier.slnx -c Release`
- 已执行 `npm run build`
- 已执行 `npx tauri build --debug`

## 风险或遗留项
- 本次已经验证了工作流中的核心本地命令，但没有在 GitHub Actions 远端实际跑一次工作流，首次推送后仍建议检查 Actions 日志与 Release 附件。
- 当前工作流会同时产出 WPF 和 Tauri 两套包，是否后续缩减为只发布 Tauri，仍需等默认入口切换决策完成。
