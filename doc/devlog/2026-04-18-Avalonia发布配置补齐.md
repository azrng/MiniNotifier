# 2026-04-18 Avalonia 发布配置补齐

## 本次目标

- 为 Avalonia 主工程补齐可复用的 Native AOT 发布配置，减少手工命令漂移。

## 核心改动

- 新增 `src/MiniNotifier.Avalonia/Properties/PublishProfiles/NativeAot-win-x64.pubxml`，固化 `Release + win-x64 + SelfContained + PublishAot + PublishTrimmed` 发布参数。
- 在 `doc/design/设计文档.md` 补充 Avalonia 发布配置应通过固定 `PublishProfile` 复用的约定。
- 更新 `TASK.md` 记录本次发布链路进展。

## 修改文件

- `src/MiniNotifier.Avalonia/Properties/PublishProfiles/NativeAot-win-x64.pubxml`
- `doc/design/设计文档.md`
- `TASK.md`

## 校验情况

- 已执行 `dotnet publish src/MiniNotifier.Avalonia/MiniNotifier.Avalonia.csproj /p:PublishProfile=NativeAot-win-x64 -c Release`
- 已对 `publish/win-x64-aot/MiniNotifier.Avalonia.exe` 做 5 秒短时启动验证，进程可正常拉起

## 风险或遗留项

- 当前仍预期存在第三方依赖 `MicroCom.Runtime` 的 `IL3053` 告警，本次改动不尝试消除此类外部包分析警告。
- 暂未启用 `StackTraceSupport=false`、UPX 压缩等更激进的体积优化参数，后续若确认优先目标是包体积，可再单独评估。
