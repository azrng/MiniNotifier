# 2026-04-19 Avalonia 主窗口底部遮挡复修

## 本次目标

- 修复 Avalonia 主窗口中部内容继续绘制到底部状态栏下方的问题。

## 核心改动

- 更新 `MainWindow.axaml`，为主内容状态容器和中间内容行开启 `ClipToBounds`。
- 将中间双列内容显式放入第 0 行，并为左右两列分别保留垂直滚动。
- 使用 `Grid.RowSpacing` 分隔中间内容和底部状态栏，移除底部栏自身上边距，避免布局高度计算含混。

## 修改文件

- `src/MiniNotifier.Avalonia/Views/MainWindow.axaml`
- `TASK.md`

## 校验情况

- 已执行 `dotnet build src/MiniNotifier.Avalonia/MiniNotifier.Avalonia.csproj -c Release`，构建成功，0 警告，0 错误。
- 已执行 `dotnet build MiniNotifier.slnx -c Release`，解决方案构建成功，0 警告，0 错误。

## 风险或遗留项

- 当前环境未做自动化桌面截图回归；修复点集中在布局裁剪与滚动边界，建议重新打开窗口确认 700px 左右高度下底部栏不再遮挡内容。
