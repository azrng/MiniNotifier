# 切换发布目标为固定开关

## 本次目标
- 将 GitHub Actions 的发布选择从“按分支拆分”改为“修改一个固定值决定当前发布 WPF 或 Tauri”。

## 核心改动
- 更新 `.github/workflows/build.yml`，新增顶层 `PUBLISH_TARGET` 开关，支持 `wpf` / `tauri` 两个固定值。
- `build-wpf` 与 `build-tauri` 两个 job 改为根据 `PUBLISH_TARGET` 决定是否执行，不再依赖分支名判断发布目标。
- 更新 `README.md`，补充当前工作流的切换方式与默认值说明。
- 更新 `TASK.md`，记录并收口本次任务状态。

## 修改文件
- `.github/workflows/build.yml`
- `README.md`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build MiniNotifier.slnx -c Release`
- 已执行 `npx tauri build --debug`
- 已重新读取改动文件，确认中文与 UTF-8 内容正常

## 风险或遗留项
- 当前工作流的触发分支列表仍保留 `main`、`tauri-rust`、`feat/tauri-rust`；本次只调整“发布哪个包”的判断方式，没有改触发范围。
- 已完成本机等价验证，但尚未在 GitHub Actions 远端实际跑一次；首次推送后仍建议检查对应 workflow 日志与 Release 结果。
