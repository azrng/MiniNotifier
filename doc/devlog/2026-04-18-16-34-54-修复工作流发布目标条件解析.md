# 修复工作流发布目标条件解析

## 本次目标
- 修复 `.github/workflows/build.yml` 中固定发布目标开关导致的 GitHub Actions 工作流解析失败，恢复 WPF / Tauri 条件分流。

## 核心改动
- 保留顶层 `PUBLISH_TARGET` 固定开关，不改现有切换方式。
- 在工作流中新增 `resolve_publish_target` 预处理 job，由它读取 `PUBLISH_TARGET` 并输出 `publish_target`。
- 将 `build-wpf` 与 `build-tauri` 的 job 条件改为读取 `needs.resolve_publish_target.outputs.publish_target`，避免在 job 级 `if` 中直接引用顶层 `env`。
- 更新 `TASK.md`，记录并收口本次任务状态。

## 修改文件
- `.github/workflows/build.yml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build MiniNotifier.slnx -c Release`
- 已执行 `npm run build`
- 已执行 `npx tauri build --debug`
- 已重新读取改动文件，确认中文与 UTF-8 内容正常

## 风险或遗留项
- 本次已修复已知的工作流表达式解析问题，但尚未在 GitHub Actions 远端实际触发一次 workflow；首次推送后仍建议检查 Actions 日志与分流结果。
- 当前环境没有现成的 `actionlint`，因此未执行专门的 workflow lint；本次主要依赖问题定位、改动后复读与本地构建等价验证。
