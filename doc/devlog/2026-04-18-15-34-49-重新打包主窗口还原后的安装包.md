# 重新打包主窗口还原后的安装包

## 本次目标
- 基于主窗口恢复系统标题栏后的当前代码重新生成一版 `release` 安装包，并确认产物时间与基础启动可用性。

## 核心改动
- 更新 `TASK.md`，登记并收口本次重新打包验证任务。
- 执行 `npm run build`，确认前端生产构建通过。
- 执行 `cargo check --manifest-path src-tauri/Cargo.toml`，确认 Rust / Tauri 编译校验通过。
- 执行 `npx tauri build`，生成新的 `release` MSI 与 NSIS 安装包。
- 启动 `src-tauri/target/release/mininotifier-tauri.exe` 做一次轻量启动验证，并在确认进程拉起后结束进程。

## 修改文件
- `TASK.md`
- `doc/devlog/2026-04-18-15-34-49-重新打包主窗口还原后的安装包.md`

## 校验情况
- 已执行 `npm run build`
- 已执行 `cargo check --manifest-path src-tauri/Cargo.toml`
- 已执行 `npx tauri build`
- 已执行 release 可执行文件启动验证，结果为 `LAUNCH_OK`
- 已确认最新 release 安装包产物时间：
  - `src-tauri/target/release/bundle/msi/MiniNotifier_0.1.0_x64_en-US.msi` -> `2026-04-18 15:34:25`
  - `src-tauri/target/release/bundle/nsis/MiniNotifier_0.1.0_x64-setup.exe` -> `2026-04-18 15:34:35`

## 风险或遗留项
- 本次仅做了轻量启动验证，未逐项人工回归托盘菜单、提醒弹窗与设置保存链路。
- 若你安装这版后仍看到旧界面，需要先卸载旧包或确认启动的不是之前残留的旧进程。
