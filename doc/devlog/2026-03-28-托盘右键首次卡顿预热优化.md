# 2026-03-28 托盘右键首次卡顿预热优化

## 本次目标
- 缓解应用启动后第一次右键托盘图标时的明显卡顿

## 核心改动
- 在 `TrayService` 中持有托盘 `ContextMenu`
- 应用启动完成后，使用 Dispatcher 空闲时机静默预热菜单模板和弹出层
- 将首次右键前的懒加载开销提前到后台空闲阶段执行

## 修改文件
- `src/MiniNotifier/Services/Implementations/TrayService.cs`
- `TASK.md`

## 校验情况
- 执行 `dotnet build MiniNotifier.slnx`，构建成功

## 风险或遗留项
- 已完成编译验证；由于当前环境无法直接进行系统托盘交互，仍需在桌面环境手工确认首次右键的体感是否达标
