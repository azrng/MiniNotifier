# 立即提醒重置下次时间

## 本次目标

- 调整“立即提醒一次”的业务逻辑：手动触发提醒后，下次正常提醒时间应从当前时间重新加上提醒间隔。

## 核心改动

- 设置窗口“立即提醒”调用提醒预览时改为 `preserveNextReminder: false`。
- 托盘菜单“立即提醒一次”调用提醒预览时改为 `preserveNextReminder: false`。
- 复用现有 `HydrationSettingsService.RecordReminderShownAsync` 逻辑重算并持久化 `NextReminderAt`。

## 修改文件

- `src/MiniNotifier/ViewModels/Windows/MainWindowViewModel.cs`
- `src/MiniNotifier/Services/Implementations/TrayService.cs`
- `TASK.md`
- `doc/devlog/2026-04-28-立即提醒重置下次时间.md`

## 校验情况

- 首次 `dotnet build MiniNotifier.slnx` 被正在运行的本项目调试进程 `MiniNotifier.exe` 锁定输出文件阻塞。
- 结束该调试进程后，`dotnet build MiniNotifier.slnx` 通过，0 警告。
- `dotnet test MiniNotifier.slnx --no-build` 已执行；仓库当前无测试项目输出。

## 风险或遗留项

- 仓库当前没有测试项目，未补充单元测试。
