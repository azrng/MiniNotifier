# 修复自定义标题栏按钮无法点击

## 本次目标
- 修复无边框主窗口标题栏中的“最小化 / 切换大小 / 收起到托盘”按钮点击无响应的问题。

## 核心改动
- 更新 `src-ui/features/settings/SettingsWindow.tsx`，将 `data-tauri-drag-region` 从整条标题栏容器移除，只保留在左侧标题信息区域。
- 双击切换窗口大小的交互也同步收敛到左侧拖拽区，避免按钮区继续被拖拽区域吞掉。
- 更新 `TASK.md`，记录并收口本次任务状态。

## 修改文件
- `src-ui/features/settings/SettingsWindow.tsx`
- `TASK.md`

## 校验情况
- 已执行 `npm run build`
- 已重新检查标题栏结构，确认按钮区不再位于拖拽区域内

## 风险或遗留项
- 本次只做了前端构建校验，未额外重新打包安装包。
- 如果你接下来还要继续安装验证，我建议直接基于当前工作区再打一版新包，避免拿到旧的 debug 安装包。
