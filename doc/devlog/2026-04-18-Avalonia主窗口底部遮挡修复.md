# 2026-04-18 Avalonia 主窗口底部遮挡修复

## 本次目标

- 修复 Avalonia 主窗口中右侧卡片区域遮挡底部状态栏的问题。

## 核心改动

- 更新 `src/MiniNotifier.Avalonia/Views/MainWindow.axaml`，将右侧“操作入口 / 应用信息”区域放入 `ScrollViewer`。
- 保持底部状态栏固定在页面底部，避免窗口高度不足时右侧内容直接溢出并覆盖底栏。
- 更新 `TASK.md` 记录本次界面修复进展。

## 修改文件

- `src/MiniNotifier.Avalonia/Views/MainWindow.axaml`
- `TASK.md`

## 校验情况

- 已执行 `dotnet build src/MiniNotifier.Avalonia/MiniNotifier.Avalonia.csproj -c Release`

## 风险或遗留项

- 当前未做自动化 UI 截图回归，只完成布局结构修复；首次手动打开窗口时仍建议顺手确认不同高度下的滚动体验。
