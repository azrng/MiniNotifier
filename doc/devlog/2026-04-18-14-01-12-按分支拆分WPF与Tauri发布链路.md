# 按分支拆分 WPF 与 Tauri 发布链路

## 本次目标
- 让 `main` 分支继续只发布 WPF 产物。
- 让 `tauri-rust` / `feat/tauri-rust` 分支单独发布 Tauri 安装包，避免双轨产物混发。

## 核心改动
- 重写 `.github/workflows/build.yml`，拆成 `build-wpf` 与 `build-tauri` 两个 job，分别按分支条件执行。
- `main` 分支保留 WPF 的 ZIP 打包与正式 Release。
- `tauri-rust` / `feat/tauri-rust` 分支改为只构建和发布 Tauri 的 MSI / NSIS，并以预发布形式输出。
- 更新 `README.md`，明确当前 CI 的分支级发布行为。
- 记录一次本机 PowerShell + `nvm` 的 PATH 异常到 `.learnings/ERRORS.md`，避免重复踩坑。

## 修改文件
- `.github/workflows/build.yml`
- `README.md`
- `.learnings/ERRORS.md`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build MiniNotifier.slnx -c Release`
- 已执行 `npm run build`
- 已执行 `npx tauri build --debug`

## 风险或遗留项
- 本次已完成本地等价验证，但尚未在 GitHub Actions 远端实际跑一次分支工作流，首次推送后仍建议分别检查 `main` 和迁移分支的 Actions 日志。
- 当前兼容了仓库现有 `feat/tauri-rust` 分支名以及未来可能的 `tauri-rust` 分支名；若后续统一改名，可再收敛条件。
