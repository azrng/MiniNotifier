# 2026-04-19 Avalonia 移除底部状态栏

## 本次目标

- 按要求移除 Avalonia 主窗口底部“状态提示”整条区域，减少底部占位和遮挡风险。

## 核心改动

- 删除 `MainWindow.axaml` 中底部 `footer-card` 状态提示区域。
- 将“退出应用”入口移动到右侧“操作入口”卡片中，避免窗口内退出入口丢失。
- 将 `StatusMessageText` 以紧凑 caption 形式放入操作卡片，保留保存、校验和提醒预览的必要反馈。

## 修改文件

- `src/MiniNotifier.Avalonia/Views/MainWindow.axaml`
- `TASK.md`

## 校验情况

- 已执行 `dotnet build src/MiniNotifier.Avalonia/MiniNotifier.Avalonia.csproj -c Release`，构建成功，0 警告，0 错误。

## 风险或遗留项

- 未做自动化截图回归；建议重新打开主窗口确认底部状态条已消失，右侧操作卡片内仍能看到保存/校验反馈。
